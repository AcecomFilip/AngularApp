import { FilipsColorDirective } from './filips-color.directive';

describe('FilipsColorDirective', () => {
  it('should create an instance', () => {
    const directive = new FilipsColorDirective();
    expect(directive).toBeTruthy();
  });
});
